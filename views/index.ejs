<%- include("header.ejs") %>
<body>
    <%- include("nav.ejs") %>
    <div class="container mt-4">
        <h2>Latest Forex Rates (Base: 1 EUR)</h2>
        <% if (rates) { %>
          <table class="table table-striped">
            <thead>
              <tr>
                <th>Currency</th>
                <th>Rate</th>
              </tr>
            </thead>
            <tbody>
              <% for (let currency in rates) { %>
                <tr>
                  <td><%= currency %></td>
                  <td><%= rates[currency] %></td>
                </tr>
              <% } %>
            </tbody>
          </table>
        <% } else { %>
          <p><%= error || "Waiting for data..." %></p>
        <% } %>
    </div>
  </body>
